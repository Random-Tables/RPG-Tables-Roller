export enum STATUS {
	UNSTARTED,
	STARTED,
	BUILT,
	FAILED
}
export enum DATA_MODE {
	ALL,
	SLOW,
	ON_REQUEST
}
export enum CHOICE_TYPE {
	string = 'string',
	npc = 'npc'
}

export enum THEME {
	fantasy = 'fantasy',
	horror = 'horror'
}